# macOS / external-drive cruft
.DS_Store
._*
.Spotlight-V100
.Trashes

# Source originals — NOT deployed. The optimized, web-ready copies live in gallery/
# and are generated by build_gallery.py. Keep the heavy full-res originals local.
photos/

# Old scaffold (superseded by app.js + data.js + build_gallery.py)
photos.js
make-manifest.sh

# Local dev/preview config
.claude/
